# nixpacks.toml
# Railway deployment configuration for Node.js with QPDF support

[phases.setup]
# Install system dependencies including QPDF for PDF encryption
aptPkgs = ["qpdf=11.9.1-1"]

[phases.install]
# Install Node.js dependencies
cmds = ["npm install"]

[start]
# Start command
cmd = "npm start"
